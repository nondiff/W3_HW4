@model IEnumerable<s1114760HW4.Models.Cloth>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.名稱)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.尺寸)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.顏色)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.現貨)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.名稱)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.尺寸)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.顏色)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.現貨)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
